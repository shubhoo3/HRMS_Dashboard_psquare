// Base API URL - in a real app, this would be loaded from environment variables
export const API_URL = 'http://localhost:5000'

// Session timeout in milliseconds (2 hours)
export const SESSION_TIMEOUT = 2 * 60 * 60 * 1000